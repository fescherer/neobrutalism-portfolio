export type Icon = {
  color?: string
  fill?: boolean
}

export type Children = {
  children: React.ReactNode
}

export type ProjectCardType = {
  id: number
  title: string
  tags: string[]
  url: string
  date: number
  description: string
}
